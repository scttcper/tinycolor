{
  "name": "@ctrl/tinycolor",
  "version": "0.0.0",
  "description": "Fast, small color manipulation and conversion for JavaScript",
  "author": "Scott Cooper <scttcper@gmail.com>",
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "license": "MIT",
  "homepage": "https://tinycolor.vercel.app",
  "repository": "scttcper/tinycolor",
  "keywords": [
    "typescript",
    "color",
    "manipulation",
    "tinycolor",
    "hsa",
    "rgb"
  ],
  "main": "dist/public_api.js",
  "module": "dist/module/public_api.js",
  "typings": "dist/public_api.d.ts",
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "scripts": {
    "demo:build": "cd demo && node --run build",
    "demo:watch": "cd demo && node --run dev",
    "lint": "eslint --ext .js,.ts, .",
    "lint:fix": "eslint --fix --ext .js,.ts, .",
    "prepare": "node --run build",
    "build": "del-cli dist && tsc -p tsconfig.build.json && tsc -p tsconfig.module.json && ts-node build",
    "docs:build": "typedoc --out demo/dist/docs --hideGenerator --tsconfig tsconfig.build.json src/public_api.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ci": "vitest run --coverage --reporter=default --reporter=junit --outputFile=./junit.xml",
    "bench": "vitest bench --run --reporter=verbose",
    "bench:watch": "vitest bench --reporter=verbose"
  },
  "devDependencies": {
    "@ctrl/eslint-config": "4.0.14",
    "@rollup/plugin-terser": "0.4.4",
    "@types/node": "24.13.3",
    "@vitest/coverage-v8": "5.0.0",
    "del-cli": "5.1.0",
    "eslint": "8.57.1",
    "rollup": "4.9.5",
    "ts-node": "10.9.2",
    "typedoc": "0.25.7",
    "typescript": "5.3.3",
    "vitest": "5.0.0"
  },
  "release": {
    "branches": [
      "master"
    ]
  },
  "engines": {
    "node": ">=14"
  },
  "packageManager": "pnpm@12.3.4"
}
